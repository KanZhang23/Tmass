SUBDIRS = histo histo_util plot_widgets util histo_api programs

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = histoscope.pc

EXTRA_DIST = histoscope.pc.in HISTORY LICENSE ORIGIN INSTALL doc

ACLOCAL_AMFLAGS = -I m4
